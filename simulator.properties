# Do not leave the coordinator waiting for five minutes when a large synchronous operation or JVM is stuck.
# A healthy run may need several seconds for in-flight calls to return after its timed phase finishes.
TEST_COMPLETION_TIMEOUT_SECONDS=60
WAIT_FOR_WORKER_SHUTDOWN_TIMEOUT_SECONDS=5
